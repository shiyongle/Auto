<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CL_User">

    <resultMap id="CLUserMap" type="com.pc.model.CL_User">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
    </resultMap>

	<!-- 用于select查询公用抽取的列 -->
	<sql id="columns">
	    <![CDATA[id ,name ,password ]]>
	</sql>
	
    <insert id="insert" >
    <![CDATA[
        INSERT INTO cl_sys_user (
        	id ,name,password
        )VALUES(
        	#{id, jdbcType=INTEGER} ,
        	#{name, jdbcType=VARCHAR} ,
        	#{password, jdbcType=VARCHAR} 
        )
    ]]>
    </insert>

    <update id="update" >
    <![CDATA[
        UPDATE cl_sys_user SET
	        name = #{name, jdbcType=VARCHAR} ,
	        password = #{password, jdbcType=VARCHAR} 
        WHERE 
	        id = #{id} 
    ]]>
    </update>

    <delete id="delete">
    <![CDATA[
        DELETE FROM cl_sys_user WHERE  id = #{id} 
    ]]>
    </delete>
    
    <select id="getById" resultMap="CLUserMap">
	    SELECT <include refid="columns"/>
	    <![CDATA[
	        FROM cl_sys_user WHERE  id = #{id} 
	    ]]>
    </select>
    
    <sql id="findWhere">
	    <where>
		   	<if test="@Ognl@isNotEmpty(id)">
			    AND id = #{id}
			</if>
            <if test="@Ognl@isNotEmpty(name)">
                AND name = #{name}
            </if>
			<if test="@Ognl@isNotEmpty(searchKey)">
			    AND (
			        name       LIKE CONCAT('%',#{searchKey},'%') 
			    )
			</if>
	    </where>
	</sql>
    
    <!-- 求满足条件的记录数 --> 
   	<select id="findPageCount" resultType="long">
        SELECT count(*) FROM cl_sys_user <include refid="findWhere"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 #offset#,#pageSize#,#lastRows# 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findPage" resultMap="CLUserMap">
    	SELECT <include refid="columns"/> FROM cl_sys_user 
		<include refid="findWhere"/>
		<if test="@Ognl@isNotEmpty(sortColumns)">
			ORDER BY ${sortColumns}
		</if>
    </select>

	<!-- 查询记录树 -->
	<select id="find" resultMap="CLUserMap">
	    SELECT <include refid="columns"/> FROM cl_sys_user 
		<include refid="findWhere"/>
		<if test="@Ognl@isNotEmpty(sortColumns)">
			ORDER BY ${sortColumns}
		</if>
    </select>
</mapper>

